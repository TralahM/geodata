os:
    - linux
sudo:
    required
language: python
install:
    - sudo apt-get update && sudo apt-get install libgeo*
    - git clone https://github.com/TralahM/geodata
    - cd geodata
    - pip install -r requirements.txt
    - python africa.py
